## Forensic Audit Report

**Work Product**: `/Users/stnava/code/syntx`
**Profile**: General Project + Syntx Registration Guardrails
**Verdict**: CLEAN

### Phase Results
- **Hardcoded output detection**: PASS — Search of the codebase and test suite confirms that tests execute actual PyTorch and JAX optimization loops. Expected outputs/Pearson correlations are checked dynamically rather than faked or hardcoded.
- **Facade detection**: PASS — Source implementations in `src/syntx/features.py`, `src/syntx/syn.py`, and `src/syntx/syn_jax.py` contain genuine feature extraction, grid composition, and optimization loops. Mocks are only used for MONAI in tests to isolate test execution from external package dependencies.
- **Pre-populated artifact detection**: PASS — Checked for pre-populated logs or artifacts. Outputs in `outputs_comparison` are output files dynamically generated by benchmark and execution runs.
- **Build and run**: PASS — Built and ran the test suite successfully (88 tests passed, 6 skipped).
- **Single Interpolation Policy**: PASS — Verified that `syn.py` and `syn_jax.py` compose initial transforms with learned affine and deformable grids entirely in coordinate space, executing a single `ants.apply_transforms` call at the end on the native-space images. No intermediate file-based or array-based pre-warping occurs.
- **VGG Feature Space Guidelines**: PASS — VGG 3D LNCC with Layer 4 (`vgg_mode='lncc_3d'`, `vgg_layers=[4]`) is set as the default in both PyTorch and JAX backend implementations and is strictly utilized for the 3D native-resolution cortical label mapping benchmarks.

### Evidence

#### 1. PyTest Execution Output
```
platform darwin -- Python 3.13.2, pytest-9.0.2, pluggy-1.5.0
rootdir: /Users/stnava/code/syntx
configfile: pyproject.toml
testpaths: tests
plugins: cov-7.1.0
collected 94 items

tests/test_coverage_helpers.py ........................                  [ 25%]
tests/test_e2e_metrics.py ...........................                    [ 54%]
tests/test_feature_networks.py ...........                               [ 65%]
tests/test_swin_unetr_empirical.py .....                                 [ 71%]
tests/test_syn.py ..ss..ss...                                            [ 82%]
tests/test_syn_jax.py ..ss..........                                     [ 97%]
tests/test_transform.py ..                                               [100%]
============ 88 passed, 6 skipped, 6 warnings in 115.52s (0:01:55) =============
```

#### 2. Single Interpolation Policy Code Details (`src/syntx/syn.py`)
- **Initial Grid Computation**:
```python
1531:     if initial_transform is not None:
1532:         tx_list = initial_transform if isinstance(initial_transform, list) else [initial_transform]
1533:         initial_grid = compute_initial_grid(fixed, moving, tx_list)
```
- **Grid Composition (PyTorch Backend)**:
```python
1022:                         if initial_grid_level is not None:
1023:                             grid = compose_grids(initial_grid_level, grid)
```
- **Single-step Warping Execution**:
```python
1743:         if initial_transform is not None:
1744:             if affine_file is not None:
1745:                 fwd_transforms = [fwd_file, affine_file] + tx_list
...
1780:     warpedmovout = ants.apply_transforms(fixed=fixed, moving=moving, transformlist=fwd_transforms)
1781:     warpedfixout = ants.apply_transforms(fixed=moving, moving=fixed, transformlist=inv_transforms, whichtoinvert=whichtoinvert_inv)
```

#### 3. VGG 3D Mode Code Details
- **PyTorch Backend Defaults (`src/syntx/syn.py`)**:
```python
832:             vgg_layers=[4], vgg_patch_size=32, vgg_num_patches=8, vgg_mode='lncc_3d',
...
1473:     vgg_layers=[4],
1474:     vgg_mode='lncc_3d',
```
- **JAX Backend Defaults (`src/syntx/syn_jax.py`)**:
```python
1040:         vgg_mode = kwargs.get('vgg_mode', 'lncc_3d')
```
